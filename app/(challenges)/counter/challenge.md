Challenge 1 — Counter with limits 🟢 Easy
Build a Counter component that:

Displays a count starting at 0
Has Increment and Decrement buttons
Count cannot go below 0 or above 10
When the count hits a limit, that button becomes disabled