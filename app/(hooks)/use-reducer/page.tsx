


export default function UseReducerPage() {
    return (
        <div>
            <h1>useReducer Hook</h1>
            <p>
                The <code>useReducer</code> hook is a powerful tool for managing complex state in React applications. It allows you to define a reducer function that takes the current state and an action, and returns a new state based on the action type. This is particularly useful when you have multiple state variables that depend on each other or when you want to implement more complex state logic.
            </p>

        </div>
    )
}